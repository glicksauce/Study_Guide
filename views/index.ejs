<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>
        <% if(currentUser) { %>
            Welcome <%=currentUser.username%>! here are you study guides:
            <% for(let i = 0; i < guides.length; i++){ %>
                <li><%=guides[i].guide_name%></li>
               
            <% } %>  
        <% } else { %>
            Hello guest, please sign in
        <% } %>
    </h1>
    <% if(currentUser) { %>
        <main>
                Welcome to the <a href="/app">party</a>
        </main>
    <% } %>
    <% if(currentUser) { %>
        <li>
            <form action="/sessions?_method=DELETE" method="POST">
                <input type="submit" value="Logout"/>
            </form>
        </li>
    <% } %>
</body>
</html>